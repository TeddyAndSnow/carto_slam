#pragma once

#include <deque>
#include <memory>

#include "common/time.h"
#include "estimator/imu_tracker.h"

namespace carto_slam {
    namespace estimator {
        class PoseExtrapolator {
        public:
            PoseExtrapolator();
        };
    }
}